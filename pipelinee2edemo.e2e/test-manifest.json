{
  "format": "pipeline-e2e-manifest-v1",
  "tests": [
    {
      "testId": "fixture.artifact-identity",
      "name": "Gate receives the immutable candidate identity",
      "labels": [
        "essential",
        "smoke"
      ]
    },
    {
      "testId": "fixture.environment-stage",
      "name": "Gate runs in the expected environment stage",
      "labels": [
        "full",
        "deployment"
      ]
    }
  ]
}
